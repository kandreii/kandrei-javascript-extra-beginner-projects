<!DOCTYPE html>
<html>
  <head></head>
  <body>

    <script>
/*   12j and 12k/*
      /*multiply = (num1, num2) =>  num1 * num2;
      
      console.log(multiply(7, 10));*/

      /*12-l*/
      /*
      countPositive = (nums) => {
        let count = 0;
        nums.forEach (num => {
          if (num > 0){
            count++;
          }
        });
        return count;
      };
      

      console.log(countPositive([1, 1, 3]));
      console.log(countPositive([-2, 3, -5, 7, 10]));*/





      /*addNumber = (array, num) => {
        
        
        pog = array.map(num1 => num1 + num);
        return pog;
      }


      console.log(addNumber([1, 2, 6], 2));*/



      /*addNum = (array, num) => {
        pog = array.map(num1 =>  num1 + num;
        );
        return pog;
      }

      console.log(addNum([1, 2, 3], 2));*/




      /*hello = [1, 1, 3];

      hell = hello.map(value =>{
        return value + 1;
      })

      console.log(hell);*/

      /* 12-n*/
      /*removeEgg = (foods) => {
        pogs = foods.filter((food) => {
          if (food != 'egg'){
            return food;
          };
        });return pogs;
      }

      console.log(removeEgg(['egg', 'apple', 'egg', 'egg', 'ham']));*/


      /* 12-o */
      /*removeEgg = (foods) => {
        let eggsRemoved = 0;

        return foods.filter ((food) => {
          if(food === 'egg' && eggsRemoved  < 3){
            eggsRemoved++;
            return false;
          };return true;
        });
        
      }
      

      console.log(removeEgg(['egg', 'apple', 'egg', 'egg','egg','ham']));*/
      /*removeItem = (fruits) => {
        let eggRemoved = 0;
        return fruits.filter((fruit) => {
          if (fruit === 'egg' && eggRemoved < 2 ){
            eggRemoved++;
            return false;
          }return true;
        })
      }


      console.log(removeItem(['egg', 'apple', 'egg', 'banana', 'egg']));*/

      

    </script>

  </body>
</html>
